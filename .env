# Copie este ficheiro para ".env" e ajuste conforme a sua instalação local do PostgreSQL

PORT=3000

PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=gpitrack

JWT_SECRET=altere-este-segredo-antes-de-usar-em-produção

# Opcional — só é necessário se quiser que o feedback também chegue por
# e-mail (sem isto, o feedback continua a ficar guardado e visível na
# Administração normalmente). Exemplo para Gmail: use uma "palavra-passe de
# aplicação" em SMTP_PASS, não a palavra-passe normal da conta.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS="sqop nppm vxza nxgx"
FEEDBACK_EMAIL_TO=vitajoao143@gmail.com
